"""
Hybrid Retriever — 混合检索工具

职责：结合关键词 + 向量 + 重排的混合检索。
"""

from typing import Dict, Any, List, Optional


def hybrid_search(query: str, filters: Optional[Dict] = None) -> List[Dict[str, Any]]:
    """混合检索（Mock）"""
    return []
